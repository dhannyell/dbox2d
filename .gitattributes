# Git stores every text file with LF and checks it out with LF on all
# platforms. A Windows clone must never receive CRLF: generated code and
# reference source compare byte for byte.
* text=auto eol=lf
